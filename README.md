# Desk

Process OS for high-stakes work queues. Plug a workflow into existing systems (core banking, IBM OpenPages, transaction monitoring, MES). Rank Identify → Prioritize → Act → Learn. The team consumes the queue their way.

```bash
npm install
npm run dev
npm run eval
```

Eval: 26 checks on deep intake (nested packets, Need more vs auto-clear, thin-payload reject).
